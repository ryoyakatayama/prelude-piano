# 曲データのパスワード保護

アプリの公開URLとサーバー管理不要の構成を維持し、写真・音符・合成WAVを暗号化します。アプリと曲名などの一覧情報は公開です。暗号化ファイル自体の取得を禁止する認証サーバーは使いません。

## 使うとき

- 共通パスワードは8文字以上。数字8桁も設定できます。
- 各端末で初回に入力し、必要に応じて「この端末で覚える」を選びます。
- 記憶するのは書き出し不可のWeb Crypto CryptoKeyです。平文パスワードは保存しません。
- 「入力の記憶を解除」は復号鍵の記憶を解除します。保存した曲は端末内に残し、パスワードなしで練習できます。
- 普通の曲データ・WAV・バックアップ書き出しは個人のファイルとして保存されます。GitHubに渡す場合は「GitHub追加用に暗号化」を使います。
- バックアップには復号鍵を含めません。別端末で新しい曲を取得する際は共通パスワードを入力します。

## 曲を追加するとき

`docs/vault.json` の公開鍵だけで暗号化できるため、採譜と音源追加を依頼するたびにパスワードを知らせる必要はありません。採譜した平文JSONと元写真はリポジトリ外の作業場所で扱い、`scripts/protect.mjs` で暗号化してからGitに保存します。生成時にパスワードも秘密鍵も使用しません。

```sh
node scripts/protect.mjs <リポジトリ外の採譜フォルダー>
node scripts/prepare.mjs
node --test tests/*.test.mjs
node scripts/check.mjs
```

公開フォルダーは暗号化済みの楽譜とWAVの対のみを許可します。カタログ・ファイル名・曲ID・鍵ID・公開メタデータの一致もチェックします。

## 暗号方式

- 共通のRSA-OAEP 3072bit / SHA-256鍵ペアをブラウザ内で作成します。
- 秘密鍵はPBKDF2-SHA256（600,000回・ランダム16byte salt）とAES-256-GCMでパスワード保護します。公開される秘密鍵情報は暗号化済みです。
- 曲と音源はそれぞれランダムなAES-256-GCM鍵と12byte IVで暗号化します。各AES鍵は公開RSA鍵で包みます。
- 鍵設定・曲ID・データ種別・公開メタデータを認証付き暗号の追加データに含め、取り違えや改変を検出します。
- 「この端末で覚える」の鍵は設定ファイルのfingerprintと照合します。設定変更時には以前の端末記憶を使用しません。
- Service Workerは曲を自動キャッシュしません。曲は明示的なダウンロード後にIndexedDBで管理します。

## 初期設定・保管

今回の設定は、パスワードをブラウザ内で処理する一時的なローカル設定画面で実施しました。設定用サーバーが受け取るのは暗号化済みの鍵設定だけです。通常の利用時にこのサーバーは不要です。

新しい独立ライブラリを作る場合のみ `node scripts/setup-vault.mjs <リポジトリ外の出力先>` を実行します。既存の `vault.json` を新しい鍵で置き換えると既存の曲はその鍵で開けなくなります。パスワード変更・鍵更新は既存曲と端末保存を考慮して別途行います。初期設定をパスワード変更の代わりに実行しないでください。

Git履歴は書き換えていません。初回公開のサンプル2曲は旧履歴とサンプル生成コードから取得可能です。新規楽譜を公開前から保護するための構成です。短いパスワードでは推測の余地があり、ブラウザの暗号化は端末所有者や既に保存された複製の利用を制御するものではありません。

参考: [MDN Web Crypto](https://developer.mozilla.org/en-US/docs/Web/API/SubtleCrypto)、[OWASP PBKDF2](https://cheatsheetseries.owasp.org/cheatsheets/Password_Storage_Cheat_Sheet.html#pbkdf2)。
