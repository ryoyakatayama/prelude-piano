{
  "name": "prelude-piano-practice",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Serverless, offline piano practice for iPad and GitHub Pages",
  "scripts": { "dev": "node scripts/serve.mjs", "test": "node --test tests/*.test.mjs", "check": "node scripts/check.mjs", "build": "node scripts/check.mjs" },
  "engines": { "node": ">=22" }
}
