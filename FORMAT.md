# Prélude曲データ v1

拡張子は `.piano.json`。UTF-8 JSONです。以下は最小の入力例です。平文の採譜データはリポジトリの外で管理します。

```json
{
  "version": 1,
  "id": "my-piece",
  "title": "曲名",
  "composer": "作曲者",
  "tempo": 80,
  "timeSignature": "4/4",
  "key": "C major",
  "fingerings": "suggested",
  "pages": [],
  "measures": [
    {
      "number": 1,
      "beats": 4,
      "notes": [
        {"id":"r1", "midi":60, "beat":0, "duration":1, "hand":"right", "finger":1},
        {"id":"r2", "midi":62, "beat":1, "duration":1, "hand":"right", "finger":2},
        {"id":"r3", "midi":64, "beat":2, "duration":2, "hand":"right", "finger":3},
        {"id":"l1", "midi":48, "beat":0, "duration":4, "hand":"left", "finger":5}
      ]
    }
  ]
}
```

## 時間軸・音程

- `id`: 半角英数字・ハイフン・アンダースコア1〜100文字。一度公開した曲のIDは維持します。
- `tempo`: 四分音符を1拍とする20〜300 BPM。
- `measures`: 実際に演奏する順の小節。反復記号や1番・2番括弧は必要に応じて小節を展開します。小節番号は取り込み時に1から振り直します。
- `beats`: 小節の四分音符換算の長さ。4/4なら4、6/8なら3、不完全小節なら実際の長さ。
- `beat`: 小節頭を0とする発音位置。四分音符1、八分音符0.5。小数を使えます。
- `duration`: 四分音符換算の音価。小節をまたがない値にします。タイをまたぐ厳密な持続再生はv1未対応なので、読取メモに残してください。
- `midi`: A0=21〜C8=108、中央のドC4=60。休符は `null`。
- `hand`: `right` または `left`。和音は同じbeatに複数の音符を置きます。
- `finger`: 任意、1〜5（親指=1）。印刷された運指があれば優先し、追加提案は `fingerings: "suggested"` とします。無理な手の拡張を前提にしません。
- `velocity`: 任意、0より大きく1以下。音量の強弱。省略時0.65。
- `x`: 任意、写真上の小節矩形の左端0〜右端1。旧版の写真座標として保持します。現在の五線譜の横位置は音価・音符密度から計算し、この値は使いません。
- 装飾や表情記号は元の写真で保持します。`transcriptionNotes` 等に読取メモを追加できますが、任意メタデータから動作は実行しません。

## 確認用の写真とページ対応

`pages` の各要素:

```json
{"id":"page-1","image":"data:image/jpeg;base64,...","width":1600,"height":2200}
```

実際の有効なPNG/JPEG/WebPのbase64を使用します。SVGや外部URLは受け付けません。アプリで写真を読み込むと長辺2400px以内のJPEGに変換します。

各小節に任意の `region` を付けられます:

```json
{"page":"page-1","x":0.1,"y":0.2,"width":0.22,"height":0.15}
```

座標はページの幅・高さに対する0〜1の割合。ピアノ譜の上下の五線と、その小節の幅を含む矩形です。写真の台形歪みが大きい場合は撮り直しや手動調整を行います。小節矩形と音符xは編集画面から修正できます。

写真は原本確認用です。選択中の1ページだけを画像表示し、シークバー・小節矩形・運指・ドレミは重ねません。`region.page` があれば「写真」へ切り替えた際に現在の小節のページを開きます。五線譜のシークバーは描画済みの音符位置と拍を使って補間します。

## チャットでの読取チェック

1. ページ順・五線・拍子・調号・反復順を確認。
2. 右手と左手を別々に採譜。臨時記号の小節内での有効範囲を反映。
3. 発音位置と音価を確認。和音は同じbeat、各声部の時間を混同しない。
4. 運指は印刷済みを優先し、追加した提案と区別。
5. 写真の順序と、小節に対応するページを確認。矩形と音符xは旧形式との互換用に保持できます。
6. 曖昧な読み取りは `transcriptionNotes` に記録し、ユーザーに確認。未確認の音を「認識済み」と断言しない。
7. `validateScore`、テスト、音源を確認してから公開。

## 曲を追加

採譜用の `<id>.piano.json` は公開リポジトリの外（例: このタスクの `work/source-scores/`）に保存します。パスワードを聞かずに、`docs/vault.json` の公開鍵で暗号化できます。

```sh
node scripts/protect.mjs ../../work/source-scores
node scripts/prepare.mjs
node --test tests/*.test.mjs
node scripts/check.mjs
```

カタログには曲ごとのSHA-256の短縮リビジョンを含みます。更新された原本はアプリの更新ボタンで取得でき、端末だけの編集を上書きする前に確認します。アプリの保存時に付与される `sourceRevision` と `modifiedAt` は曲本体の採譜情報ではありません。

公開するのは `docs/tracks/<id>.piano.enc.json` と `<id>.wav.enc.json` の対です。音符・写真・音源の中身はAES-256-GCMで暗号化し、曲ごとの鍵はRSA-OAEPで包みます。各ファイルの `metadata`（曲ID・種類・曲名・作曲者・テンポ・小節数）は一覧用に公開され、暗号認証にも含みます。その他の平文フィールドは検証で拒否します。暗号化済みファイルをアプリから受け取った場合はそのままtracksへ置き、prepare/checkを実行します。

`vault.json` の秘密鍵部分は、パスワードからPBKDF2-SHA256（600,000回）で導いた鍵によりAES-GCMで保護されています。設定ファイルを別端末から取得し、同じパスワードで開きます。公開用ファイルに平文パスワードや復号済み秘密鍵を含めません。仕様詳細は `docs/crypto.js` が正本です。

## 採譜の初版

`transcriptionNotes` に文字列の配列を指定すると、練習画面の「取り込みメモ・この音源について」に表示します。初版の読取状況、一定テンポへの置き換え、運指提案などを記載します。音源再生は `tempo` の一定テンポです。曲中の tempo、rit.、フェルマータ、アルペジオの自動解釈は行いません。原譜の記号は写真を参照します。

## 記譜用の音価（任意）

`notation: {"durations":[0.25],"grace":true}` は、小さな16分の装飾音です。`beat` と `duration` は再生用の時間を維持し、短い演奏時間から通常の64分音符を推測しません。休符や装飾音の直後の主音も、単一の記譜音価を指定できます。

`notation: {"durations":[0.25,1]}` は16分音符と四分音符をタイでつなぎます。配列の合計は `duration` と一致させます。再生は1つの持続音のままで、描画だけを分割します。編集画面で音の拍位置・長さを変更すると、この補助記譜情報は解除します。対応する音価が不明な場合は「音価確認」と表示します。
