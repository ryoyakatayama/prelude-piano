# 検証記録

2026-09-12。実機のiPad / 生ピアノを使う検証ではなく、ローカルブラウザとNode.jsで実施。

## 確認済み

- 音符の入力検証、拍と小節の境界、左右の手、WAVヘッダー: 自動テスト成功。
- 合成した単音・倍音の入力からC3〜C6の音程を識別。保持音で追従が連続誤進行しないことを確認。
- リピート終了点でのカーソルの折り返し、タッチつまみの小節への吸着と交差制限: 自動テスト成功。
- ブラウザで曲をダウンロード → 練習 → 再生・停止 → ドレミ表示。
- iPad相当1024×768、768×1024で表示確認。縦向きの再生バーは画面内に配置。
- 楽譜上のBつまみを4小節→2小節へドラッグ。続いて次段の6小節までドラッグし、数値入力と選択範囲の一致を確認。
- 画像・音符位置を含む検証用曲データをインポート。写真上の小節をクリックして移動。
- 検証用曲の運指を変更 → 保存 → 写真ラベルで変更を確認。
- ブラウザのWAV生成が完了し、書き出し完了表示を確認。
- ローカルHTTPサーバーを停止してアプリを再読み込み。保存曲の表示・再生を確認。
- 同状態でサンプル曲を端末から削除。GitHub側の曲一覧は維持し、削除曲を未保存として表示。
- WebMCPの一覧取得・範囲設定を登録。正しい設定の反映、不正な入力の拒否を確認。
- JavaScript構文、カタログの曲ID、画像・アプリ資産の参照、noindex、Service Worker資産の整合性をチェック。

## 実機で確認する項目

- iPad Safariのマイク権限、オーディオ出力、ホーム画面への追加。
- 実際のピアノ、端末の置き場所、残響、ペダル、テンポごとの追従精度。
- iPadの複数指操作と長いページでのドラッグ。ローカル検証ではPointer Eventsのドラッグで確認。
- iPadの「ファイル」アプリを使うバックアップ保存・復元。

## 公開

2026-09-12、ユーザーの明示承認を受けて公開リポジトリ ryoyakatayama/prelude-piano にアップロードし、GitHub Pagesで公開しました。

- 公開URL: https://ryoyakatayama.github.io/prelude-piano/
- GitHub Actions: https://github.com/ryoyakatayama/prelude-piano/actions/runs/34680715626 （Success、23秒）。テスト・資産生成・チェック・デプロイを完了。
- 公開リポジトリを取得し、35ファイルの内容がローカル成果物と一致することを確認（テキストの改行コード差を除外）。
- 公開URLでサンプル2曲の一覧、曲のダウンロード、楽譜表示、再生カーソルの進行を確認。
- 公開URLでBつまみを4小節から2小節へドラッグし、選択範囲・数値の反映を確認。
- 公開URLで曲を端末から削除し、GitHub原本の一覧と再ダウンロード操作が残ることを確認。
- 公開HTMLのnoindex指定と「オフライン準備完了」を確認。実際のオフライン再読み込みは前述のローカルHTTPサーバー停止で検証。
- GitHub側に公式ActionsのNode.js 20ランタイムに関する非致命的な移行警告がありました。Node.js 24への自動切替で全ステップは成功しています。

## 1.1 パスワード保護（ローカル検証）

- ユーザーがローカル設定画面で共通パスワードを設定。設定処理が受け取るのは暗号化された鍵設定のみ。
- 数字8桁での初期設定と復号成功、7文字の設定拒否を自動検証。
- 音符・写真を含むバイト列、音源の暗号化・復号、誤ったパスワード・鍵・曲ID・種別・改変データの拒否を自動検証。
- 非抽出CryptoKey、公開鍵のみでの曲追加、想定外の平文フィールドの拒否を確認。既存8件と合わせて12テスト成功。
- 本番とは別の検証用鍵でブラウザ動作を検証。本番パスワードは使用・取得していない。
- ダウンロード時の入力画面、誤入力後の再入力、端末への記憶、再読み込み後の入力省略を確認。
- 記憶の解除で未保存曲に再入力を要求。キャンセル後、保存曲はそのまま再生可能。
- 楽譜・音符とWAVの暗号化書き出しの完了を画面で確認。
- ローカルサーバーを停止後にアプリを再読み込みし、保存曲の再生を確認。
- 768×1024の表示を確認。ライブラリとパスワード案内に横はみ出しなし。
- 本番用暗号化ファイルの構造、楽譜・音源の対、カタログ参照、公開対象に平文の曲・WAVがないことをcheckで確認。

## 1.1 公開側の検証（2026-09-12）

- PR: https://github.com/ryoyakatayama/prelude-piano/pull/1 。全43ファイルがローカル成果物と一致することを確認し、PRのvalidate成功後にmainへマージ。
- 公開コミット: `95f48399a395000270d7a388c868294921f7f99b`。
- GitHub Actions: https://github.com/ryoyakatayama/prelude-piano/actions/runs/34683099356 （Success、28秒）。validate・deployとも成功。
- 公開URLで「パスワードで保護」の曲一覧2件、ダウンロード時の入力画面、「この端末で覚える」、キャンセルを確認。
- vault.json・catalog.json・main.js・crypto.js・vault.jsの公開内容が成果物と一致。本番パスワードは取得せず、公開先での復号はユーザーの入力で行う。
- 旧配信URLのjoy.piano.json・joy.wav・scale.piano.json・scale.wavは全てHTTP 404。Git履歴は維持。
- 公開HTMLのnoindexを確認。旧版の更新案内後、タブを閉じて再度開くと拡張版が読み込まれ、「オフライン準備完了」を表示。
- iPad実機の操作・生ピアノ追従は未実施。768×1024表示と暗号化・オフライン動作は上記の検証用ブラウザで確認済み。

## インストールとiPad音声の改善（2026-09-12）

- ライブラリ、上部ボタン、使い方からインストール可能。対応ブラウザーのネイティブ案内とiPad / Android / PCの手順を用意。iPadの共有アイコン、Apple公式手順、URLコピーをブラウザーで確認。
- Audio Session APIでは playback を選択。再生タップ内でresumeと無音の初期化を開始し、中断・背景移動後はAudioContextを再生成。マイク利用時はautoへ戻す。
- 音量スライダーはinputイベントで1%刻みの即時反映。Gainの変更は時定数20msで平滑化し、設定保存をまとめる。ブラウザーで80%→50%の表示・保存を確認。
- 再生ボタンのネイティブpaddingを解除。48pxのボタンとSVGの中心差はX / Yとも0px。三角形の重心もSVG中心に配置。
- 確認音の開始、検証用サンプル曲の取り込み・再生 / 停止をブラウザーで確認。
- 自動テスト19件成功。prepare / check成功。iPad実機のスピーカーからの発音、OSのネイティブインストール完了は未検証。
- 参考: https://github.com/w3c/audio-session/blob/main/explainer.md 、https://bugs.webkit.org/show_bug.cgi?id=252746 、https://bugs.webkit.org/show_bug.cgi?id=291892 。

## ユーザーによる確認（2026-09-12）

iPad音声・再生アイコン・音量調節の修正後、ユーザーから「問題なく動作しました」と回答あり。これはユーザーの実機確認であり、エージェントによる実機操作ではありません。

## 2曲の取り込み・和音表示・確認用写真（2026-09-12）

- Merry Christmas Mr. Lawrence（坂本龍一）: 原譜10〜15ページ、6画像、94小節、408拍。Summer（久石譲）: 原譜2〜9ページ、8画像、131小節、518拍。
- 全小節の右手・左手の採譜データを作成し、音符の範囲・拍数・各声部の長さを検査。元画像は作業フォルダー側で保持。見開きの左右を分割し、確認画像14ページの向きと小節対応を目視確認。
- 採譜の初版として登録。音高・臨時記号の最終校訂を保証するものではない。一定テンポ、タイの再発音、装飾・トレモロの近似、提案運指、Summerの64〜81小節の確認事項を「取り込みメモ・この音源について」に明記。
- 全音符データと生成WAVを、本番と独立した検証用鍵で暗号化・復号してバイト一致を確認。本番用は既存vaultの公開鍵のみで暗号化。パスワード・本番秘密鍵は取得していない。
- WAV: Merry Christmas Mr. Lawrence 291.93秒、Summer 389秒（末尾余韻を含む）。非無音・ヘッダー・サンプル数を検査し、両曲ともクリッピング0。アプリ内は音符から合成再生するため、WAV自体の端末ダウンロードは不要。
- 同時刻・同音価の和音を共通の符幹で描画。2度の音頭を左右に分け、短い音価は拍ごとの連桁にまとめる。異なる音価の声部は分離。付点・連符の音価、臨時記号の配置、音符密度に応じた改行、ドレミの縦積みを追加。
- 写真は確認用として選択中の1ページのみ画像表示。画像上のシークバー・運指・ドレミ・A/Bつまみを廃止。通常は五線譜を開き、写真モード以外では画像要素を作成しない。再生位置の更新時に曲全体を複製する処理も削除。
- ブラウザーで2曲の読み込み、Summerの和音が続く箇所、Merry Christmas Mr. Lawrenceの57小節付近を目視。768×1024の画面で五線譜・ドレミを確認。密集した小節は音符を潰さず横スクロールする。これは表示サイズの検証で、iPad実機操作ではない。
- 57〜58小節を120 BPMで複数回リピートし、範囲内の再生位置とカーソル1本を確認。ローカルサーバーを停止して再読み込み後、両曲を開き、Summerの最終写真1枚（画像上のSVG・つまみ0件）とMerry Christmas Mr. Lawrenceの再生を確認。
- 自動テスト26件成功。和音の共通符幹、八分和音の連桁、複声部、付点・連符、2度の音頭、写真座標に依存しない五線譜位置、写真に重ね描きしないことを含む。prepare / checkで4組の暗号化ファイル、noindex、平文曲の不在、オフライン資産を検査。
- 譜刻上の声部分離の参考: https://lilypond.org/doc/v2.24/Documentation/notation/single-voice 。表示は依存ライブラリを追加せず、アプリ内SVGで実装。

## 2026-09-12 音価表示の再確認

2曲225小節の音価を再走査。Merry Christmas Mr. Lawrence の28・48・80小節にある6つの装飾音を原写真と照合し、通常の64分表示から小さい16分表示へ修正。タイで合計されていた音価は同曲2音、Summer34音に記譜用の分割を追加しました（和音の各構成音を個別集計）。29テスト成功。再生のイベント・時間・音高は修正前と一致し、既存WAVをそのまま利用します。全音高の再校正を完了したという意味ではありません。
